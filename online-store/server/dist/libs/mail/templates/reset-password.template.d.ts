import * as React from 'react';
interface ResetPasswordTemplateProps {
    domain: string;
    token: string;
}
export declare function ResetPasswordTemplate({ domain, token }: ResetPasswordTemplateProps): React.JSX.Element;
export {};

import * as React from 'react';
interface ConfirmationTemplateProps {
    domain: string;
    token: string;
}
export declare function ConfirmationTemplate({ domain, token }: ConfirmationTemplateProps): React.JSX.Element;
export {};
